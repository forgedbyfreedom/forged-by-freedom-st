name: Build and Upload Master Transcripts (Dual Repo)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"  # Runs daily at 6 AM UTC

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout FBF main repo
        uses: actions/checkout@v4

      - name: 🔧 Setup Git identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: 🔍 Debug token visibility
        run: |
          if [ -z "${{ secrets.FBF_PAT }}" ]; then
            echo "❌ FBF_PAT secret not found or empty!"
          else
            echo "✅ FBF_PAT exists (length check):"
            echo "${{ secrets.FBF_PAT }}" | wc -c
          fi

      - name: 🔐 Inject PAT into Git credential helper
        run: |
          echo "🔐 Configuring Git credentials for thinkbig-transcripts..."
          git config --global credential.helper store
          echo "https://x-access-token:${{ secrets.FBF_PAT }}@github.com" > ~/.git-credentials
          cat ~/.git-credentials | sed 's/.\{10\}$/********/' || true
          echo "✅ Git credentials injected."

      - name: 📦 Clone thinkbig-transcripts repo (using stored creds)
        run: |
          echo "📦 Cloning thinkbig-transcripts..."
          git clone https://github.com/forgedbyfreedom/thinkbig-transcripts /tmp/thinkbig-transcripts || (
            echo "⚠️ Clone failed, retrying..." && sleep 10 &&
            git clone https://github.com/forgedbyfreedom/thinkbig-transcripts /tmp/thinkbig-transcripts
          )

          if [ ! -d "/tmp/thinkbig-transcripts" ]; then
            echo "❌ Failed to clone thinkbig-transcripts after retries."
            exit 1
          fi
          echo "✅ Clone complete."
          ls -lah /tmp/thinkbig-transcripts | head

      - name: 🧠 Combine transcripts into master file
        run: |
          mkdir -p transcripts
          find /tmp/thinkbig-transcripts -type f -name "*.txt" -print0 | xargs -0 cat > transcripts/master_transcript.txt || true
          echo "✅ Master transcript built."
          wc -l transcripts/master_transcript.txt || echo "⚠️ No transcript lines found."

      - name: 🗂️ Commit and push transcript updates
        run: |
          if [ -s transcripts/master_transcript.txt ]; then
            git add transcripts/master_transcript.txt
            git commit -m "Auto rebuild transcripts [skip ci]" || echo "No changes to commit."
            git push origin main
            echo "✅ Changes pushed to main branch."
          else
            echo "⚠️ No transcript data found — skipping commit."
          fi

      - name: 📤 Upload master transcript artifact
        uses: actions/upload-artifact@v4
        with:
          name: master_transcript
          path: transcripts/master_transcript.txt
